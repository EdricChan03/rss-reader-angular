export interface KeyboardShortcut {
  /**
   * An action of what the keyboard shortcut does
   */
  action: string;
  /**
   * The keyboard shortcut as an array of strings
   */
  keyboardShortcut: string[];
}
